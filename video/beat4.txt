One more thing. The interactive path is a Strands agents-as-tools orchestrator on Bedrock AgentCore, so I can just ask it things. Summarise today's proposals, and flag anything over budget. It consults the proposal log and the live budgets, and answers in a couple of sentences -- here, calling out travel at thirty-eight hundred pounds against a five hundred pound limit. AgentCore Memory keeps that context across runs.

That is the loop: deterministic rules on the edges, a Strands agent in the middle, one decision at a time. It is all on GitHub -- Apache-2.0, with a test suite that runs with no AWS access. Household Chief of Staff. It watches, it decides, it justifies -- and it asks first.
