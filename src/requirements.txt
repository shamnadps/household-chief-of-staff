# Runtime dependencies for the Lambda functions (SAM builds from this dir).
# The AgentCore Runtime container adds bedrock-agentcore on top — see
# infra/agentcore/requirements.txt.
strands-agents>=1.0.0
strands-agents-tools>=0.1.0
boto3>=1.35.0
fastapi>=0.115.0
mangum>=0.17.0
pydantic>=2.7.0
requests>=2.32.0
